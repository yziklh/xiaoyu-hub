# 📦 版本发布流程文档

本文档详细说明了 Tauri Vue Template 项目的版本更新和发布流程。

---

## 📋 目录

- [版本号规范](#版本号规范)
- [版本更新步骤](#版本更新步骤)
- [Tag 创建和推送](#tag-创建和推送)
- [GitHub Actions 自动构建](#github-actions-自动构建)
- [Draft Release 检查和发布](#draft-release-检查和发布)
- [常见问题和故障排除](#常见问题和故障排除)
- [快速参考](#快速参考)

---

## 📌 版本号规范

本项目遵循 [语义化版本（Semantic Versioning）](https://semver.org/lang/zh-CN/) 规范：

### 版本格式：`MAJOR.MINOR.PATCH`

- **MAJOR（主版本号）**：不兼容的 API 修改
- **MINOR（次版本号）**：向下兼容的功能性新增
- **PATCH（修订号）**：向下兼容的问题修正

### 示例

- `1.0.0` → `1.0.1`：修复 bug
- `1.0.1` → `1.1.0`：新增功能
- `1.1.0` → `2.0.0`：重大更新，可能不兼容

### 预发布版本（可选）

- `1.0.0-alpha.1`：内部测试版本
- `1.0.0-beta.1`：公开测试版本
- `1.0.0-rc.1`：候选发布版本

---

## 🔄 版本更新步骤

### 1. 确定新版本号

根据本次更新的内容，确定新的版本号。例如：

- 当前版本：`1.0.0`
- 新版本：`1.0.1`（修复 bug）

### 2. 更新配置文件

**必须同时更新以下三个文件**，确保版本号一致：

#### 2.1 更新 `package.json`

```bash
# 手动编辑或使用 npm version 命令
npm version 1.0.1 --no-git-tag-version
```

或手动编辑 `package.json`：

```json
{
  "name": "tauri-vue-template",
  "version": "1.0.1",  // 修改这里
  ...
}
```

#### 2.2 更新 `src-tauri/Cargo.toml`

编辑 `src-tauri/Cargo.toml`：

```toml
[package]
name = "tauri-vue-template"
version = "1.0.1"  # 修改这里
description = "A modern desktop application template..."
...
```

#### 2.3 更新 `src-tauri/tauri.conf.json`

编辑 `src-tauri/tauri.conf.json`：

```json
{
  "$schema": "https://schema.tauri.app/config/2",
  "productName": "Tauri Vue Template",
  "version": "1.0.1",  // 修改这里
  ...
}
```

### 3. 验证版本一致性

运行以下命令验证所有文件的版本号是否一致：

```bash
# 检查 package.json
grep '"version"' package.json

# 检查 Cargo.toml
grep '^version = ' src-tauri/Cargo.toml | head -1

# 检查 tauri.conf.json
grep '"version"' src-tauri/tauri.conf.json
```

**确保三个文件的版本号完全一致！**

### 4. 提交版本更新

```bash
# 添加修改的文件
git add package.json src-tauri/Cargo.toml src-tauri/tauri.conf.json

# 提交更改
git commit -m "chore: bump version to 1.0.1"

# 推送到远程仓库
git push origin main
```

---

## 🏷️ Tag 创建和推送

### 1. 创建 Git Tag

```bash
# 创建带注释的 tag（推荐）
git tag -a v1.0.1 -m "Release v1.0.1"

# 或创建轻量级 tag
git tag v1.0.1
```

### 2. 推送 Tag 到远程仓库

```bash
# 推送单个 tag
git push origin v1.0.1

# 或推送所有 tags
git push origin --tags
```

### 3. 验证 Tag

```bash
# 查看本地 tags
git tag -l

# 查看远程 tags
git ls-remote --tags origin
```

### ⚠️ 重要提示

- **Tag 推送后会自动触发 GitHub Actions 构建流程**
- **Tag 名称必须以 `v` 开头**（如 `v1.0.1`）
- **Tag 版本号必须与配置文件中的版本号一致**

---

## 🤖 GitHub Actions 自动构建

推送 Tag 后，GitHub Actions 会自动启动构建流程。

### 工作流程概览

```
Tag 推送 (v1.0.1)
    ↓
┌─────────────────────────────────────┐
│ Job 1: generate-changelog           │
│ - 提取 tag 和 version               │
│ - 验证版本一致性                    │
│ - 生成汉化 changelog                │
│ - 过滤 CI/CD commits                │
└─────────────────────────────────────┘
    ↓
┌─────────────────────────────────────┐
│ Job 2: publish-tauri (并行)         │
│ ├─ macOS (Apple Silicon)            │
│ ├─ macOS (Intel)                    │
│ └─ Windows (x64)                    │
│                                     │
│ 每个平台：                          │
│ - 安装依赖                          │
│ - 构建应用                          │
│ - 验证构建产物                      │
│ - 上传到 Draft Release              │
└─────────────────────────────────────┘
    ↓
┌─────────────────────────────────────┐
│ Job 3: notify-completion            │
│ - 显示构建摘要                      │
│ - 提供下一步操作指引                │
└─────────────────────────────────────┘
```

### 查看构建进度

访问 GitHub Actions 页面：

```
https://github.com/YOUR_USERNAME/Tauri-Vue-Template/actions
```

### 构建阶段说明

#### 阶段 1：generate-changelog

**功能**：
- 提取 tag 名称和版本号
- 验证 `package.json`、`Cargo.toml`、`tauri.conf.json` 版本一致性
- 生成汉化的 changelog
- 自动过滤 CI/CD 相关的 commits

**预期输出**：
```
📋 版本检查：
  Tag version: 1.0.1
  package.json: 1.0.1
  Cargo.toml: 1.0.1
  tauri.conf.json: 1.0.1
✅ 所有版本一致: 1.0.1

Generating changelog from v1.0.0 to v1.0.1
```

**如果版本不一致**：
```
❌ 版本不一致！
```
构建将失败，需要修复版本号后重新推送 tag。

#### 阶段 2：publish-tauri

**功能**：
- 并行构建三个平台的安装包
- 使用优化的 Rust 缓存
- 验证构建产物是否生成
- 上传到 Draft Release

**预期输出**（每个平台）：
```
🔍 验证构建产物...
✅ Windows 安装程序已生成
-rw-r--r-- 1 runner docker 15M ... Tauri Vue Template_1.0.1_x64-setup.exe
```

**构建产物**：
- Windows: `Tauri Vue Template_1.0.1_x64-setup.exe`
- macOS (Apple Silicon): `Tauri Vue Template_1.0.1_aarch64.dmg`
- macOS (Intel): `Tauri Vue Template_1.0.1_x64.dmg`

#### 阶段 3：notify-completion

**功能**：
- 显示构建摘要
- 提供下一步操作指引
- 提供快速链接

**预期输出**：
```
## 🎉 构建完成！

### 📦 Release 信息
- **版本**: v1.0.1
- **状态**: Draft（需要手动发布）

### ✅ 构建平台
- ✅ Windows (x64)
- ✅ macOS (Apple Silicon)
- ✅ macOS (Intel)

### 📝 下一步操作
1. 访问 Releases 页面
2. 检查 Draft Release 的内容和构建产物
3. 确认无误后，点击 Publish release 发布
```

### 构建时间

- **首次构建（无缓存）**：约 20-25 分钟
- **后续构建（有缓存）**：约 15-20 分钟

---

## ✅ Draft Release 检查和发布

### 1. 访问 Releases 页面

构建完成后，访问：

```
https://github.com/YOUR_USERNAME/Tauri-Vue-Template/releases
```

### 2. 检查 Draft Release

找到标记为 **Draft** 的 Release（通常在最上方），检查以下内容：

#### 2.1 Release 信息

- **Tag**：`v1.0.1`
- **Title**：`Tauri Vue Template v1.0.1`
- **Status**：Draft（草稿）

#### 2.2 Changelog 内容

确认 changelog 包含以下部分：

```markdown
## 🎉 Tauri Vue Template v1.0.1

### 📝 更新内容
- 更新内容 1 (commit_hash)
- 更新内容 2 (commit_hash)
...
（应该已自动过滤 CI/CD 相关的 commits）

### 📦 安装说明
请根据您的操作系统下载对应的安装包：
- **Windows**: `.exe` 安装程序（NSIS）
- **macOS (Apple Silicon)**: `.dmg` 适用于 M1/M2/M3 芯片的 Mac
- **macOS (Intel)**: `.dmg` 适用于 Intel 芯片的 Mac

### 🔗 相关链接
- [项目仓库](...)
- [使用文档](...)
- [完整变更](...)
```

#### 2.3 构建产物

确认包含 **3 个安装包**：

- ✅ `Tauri Vue Template_1.0.1_x64-setup.exe` (Windows)
- ✅ `Tauri Vue Template_1.0.1_aarch64.dmg` (macOS Apple Silicon)
- ✅ `Tauri Vue Template_1.0.1_x64.dmg` (macOS Intel)

### 3. 发布 Release

确认无误后：

1. 点击 **"Edit"** 按钮（如果需要修改）
2. 点击 **"Publish release"** 按钮
3. Release 将公开可见

### 4. 验证发布

发布后，验证：

- ✅ Release 不再显示 "Draft" 标记
- ✅ 所有用户可以访问和下载
- ✅ Release 出现在项目首页的 "Releases" 部分

---

## 🔧 常见问题和故障排除

### 问题 1：版本不一致错误

**错误信息**：
```
❌ 版本不一致！
```

**原因**：
`package.json`、`Cargo.toml`、`tauri.conf.json` 中的版本号不一致。

**解决方法**：
1. 检查三个文件的版本号
2. 确保完全一致（包括格式）
3. 提交更改
4. 删除旧 tag：`git tag -d v1.0.1 && git push origin :refs/tags/v1.0.1`
5. 重新创建 tag：`git tag v1.0.1 && git push origin v1.0.1`

---

### 问题 2：构建产物验证失败

**错误信息**：
```
❌ Windows 安装程序未找到！
```
或
```
❌ macOS DMG 未找到！
```

**原因**：
构建过程失败，未生成安装包。

**解决方法**：
1. 查看 GitHub Actions 日志，找到具体错误
2. 常见原因：
   - Rust 编译错误
   - 依赖安装失败
   - 磁盘空间不足
3. 修复问题后重新推送 tag

---

### 问题 3：Changelog 为空或格式错误

**原因**：
- 没有找到上一个 tag
- Git 历史记录问题

**解决方法**：
1. 确保至少有一个之前的 tag
2. 检查 `git log` 输出是否正常
3. 如果是第一个版本，changelog 会包含所有 commits

---

### 问题 4：Tag 推送后没有触发构建

**原因**：
- Tag 名称格式不正确
- GitHub Actions 工作流被禁用

**解决方法**：
1. 确保 tag 名称以 `v` 开头（如 `v1.0.1`）
2. 检查 `.github/workflows/release.yml` 是否存在
3. 检查 GitHub Actions 是否启用（Settings → Actions）

---

### 问题 5：构建时间过长

**原因**：
- 缓存未命中
- 网络问题

**解决方法**：
- 首次构建通常较慢（20-25 分钟）
- 后续构建会使用缓存（15-20 分钟）
- 如果持续过慢，检查 GitHub Actions 日志

---

## 📚 快速参考

### 完整发布流程（一键复制）

```bash
# 1. 更新版本号（假设新版本为 1.0.1）
npm version 1.0.1 --no-git-tag-version

# 2. 手动更新 src-tauri/Cargo.toml 和 src-tauri/tauri.conf.json
# （使用编辑器修改版本号为 1.0.1）

# 3. 验证版本一致性
grep '"version"' package.json
grep '^version = ' src-tauri/Cargo.toml | head -1
grep '"version"' src-tauri/tauri.conf.json

# 4. 提交更改
git add package.json src-tauri/Cargo.toml src-tauri/tauri.conf.json
git commit -m "chore: bump version to 1.0.1"
git push origin main

# 5. 创建并推送 tag
git tag v1.0.1
git push origin v1.0.1

# 6. 访问 GitHub Actions 查看构建进度
# https://github.com/YOUR_USERNAME/Tauri-Vue-Template/actions

# 7. 构建完成后，访问 Releases 页面检查并发布
# https://github.com/YOUR_USERNAME/Tauri-Vue-Template/releases
```

### 重要链接

- **GitHub Actions**: `https://github.com/YOUR_USERNAME/Tauri-Vue-Template/actions`
- **Releases**: `https://github.com/YOUR_USERNAME/Tauri-Vue-Template/releases`
- **语义化版本规范**: https://semver.org/lang/zh-CN/

### 检查清单

发布前请确认：

- [ ] 所有配置文件版本号一致
- [ ] 代码已提交并推送到 main 分支
- [ ] Tag 名称格式正确（`v` + 版本号）
- [ ] GitHub Actions 构建成功
- [ ] Changelog 格式正确且内容完整
- [ ] 所有平台的构建产物都已生成
- [ ] Draft Release 内容检查无误

---

## 📞 获取帮助

如果遇到问题：

1. 查看本文档的 [常见问题和故障排除](#常见问题和故障排除) 部分
2. 查看 GitHub Actions 构建日志
3. 提交 Issue 到项目仓库

---

**最后更新**：2025-01-06

